# 実機確認の項目

対象端末: Pixel 10 Pro / Android 17 / API 37

`🔴` は必ず確認する。ここが通らなければリリースできない。

## 🔴 鳴動が確実に起こること

| 項目 | 確認方法 | 結果 |
|---|---|---|
| Doze中に鳴る | `adb shell dumpsys deviceidle force-idle` でDozeへ入れ、予約時刻を待つ | 未実行 |
| マナーモードで鳴る | サイレント／バイブのみに設定して鳴らす。`USAGE_ALARM` なので鳴るはず | 未実行 |
| ロック画面の上に出る | 画面ロック状態で鳴動時刻を迎える | 未実行 |
| 全画面で出る | 通知バーに留まらず、鳴動画面が前面に出ること | 未実行 |
| 再起動後も鳴る | 予約後に端末を再起動し、予約が復元されること | 未実行 |

## 🔴 データが消えないこと

| 項目 | 確認方法 | 結果 |
|---|---|---|
| マイグレーション | `AlarmDatabaseMigrationTest` を実機で実行 | 未実行 |

## 🟡 権限まわり

| 項目 | 確認方法 | 結果 |
|---|---|---|
| 通知権限の要求 | 初回起動で `POST_NOTIFICATIONS` を聞かれること | 未実行 |
| 通知を拒否した場合 | 拒否した状態で鳴動時刻を迎え、どうなるか観察する | 未実行 |
| 全画面インテントの許可 | Android 14+ の `USE_FULL_SCREEN_INTENT` の状態で挙動が変わらないか | 未実行 |

## 🟡 予約の正しさ

| 項目 | 確認方法 | 結果 |
|---|---|---|
| 次の1回だけ登録 | `adb shell dumpsys alarm | grep termalarm` で登録数を見る | 未実行 |
| 停止後の再予約 | 停止操作の後、次の時刻へ更新されること | 未実行 |
| 当日終了 | 実行後、次のセッションへ予約が移ること | 未実行 |
| タイムゾーン変更 | 端末のタイムゾーンを変え、予約が再計算されること | 未実行 |

## 🟡 見た目

| 項目 | 確認方法 | 結果 |
|---|---|---|
| Dynamic Color | 壁紙を変えてアプリの色が追従すること | 未実行 |
| Google Sans Flex | 数字がGoogle Sans Flexで表示されること | 未実行 |
| 日本語のフォールバック | 日本語が豆腐にならず標準フォントで出ること | 未実行 |
| 等幅数字 | 時刻の桁が変わっても幅が動かないこと | 未実行 |
| ダークテーマ | 端末をダークにして配色が破綻しないこと | 未実行 |

## 🟢 異常系

| 項目 | 確認方法 | 結果 |
|---|---|---|
| 音源が無効 | `soundUri` の指す音が削除された状態で鳴らす。無音で鳴り続けないこと | 未実行 |
| 10分の自動停止 | 放置して10分後に鳴り止み、次回が予約されること | 未実行 |
| ゲームの各種類 | 6種類すべてを出し、正解・不正解が正しく判定されること | 未実行 |
| センサー無し端末 | 「端末を振る」が除外されること（Pixel 10 Pro にはセンサーがあるため、除外リストの動作をコードで確認する） | 未実行 |
